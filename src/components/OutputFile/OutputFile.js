import React, { Component } from 'react';

class OutputFile extends Component {
  constructor( props ) {
    super( props );
  }
  render() {
    return (
      <div>OutputFile -- Stateful</div>
    );
  }
}

export default OutputFile;
